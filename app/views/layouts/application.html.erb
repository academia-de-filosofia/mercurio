<!DOCTYPE html>
<html>
<head>
	<title>Mercúrio - Academia de Filosofia Livre</title>
	<%= javascript_include_tag :all %>	
	
	<%= css3buttons_stylesheets %>	
	<%= stylesheet_link_tag 'menu.css', :media => 'screen, projection' %>	
	<%= stylesheet_link_tag 'compiled/screen.css', :media => 'screen, projection' %>
	<%= stylesheet_link_tag 'compiled/print.css', :media => 'print' %>
	<!--[if lt IE 8]>
	<%= stylesheet_link_tag 'compiled/ie.css', :media => 'screen, projection' %>
	<![endif]-->
	<%= stylesheet_link_tag 'compiled/application.css', :media => 'screen' %>	
	<%= csrf_meta_tag %>
</head>
<body>
	<div class="container">

		<div id="header">
			<div class="span-4"><img src="/images/logo.gif" /></div>
			<div class="span-20 last">
				<% if user_signed_in? %>
					<%= render "shared/menu" %>
				<% end %>
			</div>
		</div>
		
		<% if notice %>
			<div class="clear notice"><%= notice %></div>
		<% end %>
		
		<% if alert %>
			<div class="clear error"><%= alert %></div>
		<% end %>

		<div class="span-24 last" id="content">

			<div id="main">
				<br/>
				<% if !user_signed_in? %>
					<%= render "shared/login" %>
				<% else %>
					<%= yield %>
				<% end %>  
			</div>
		</div>
		
		<div id="footer" class="clear span-24">
			<span class="prepend-19"></span>
			<span class="span-5 last">Mercúrio - Academia de Filosofia</span>
		</div>
	</div>
</body>
</html>