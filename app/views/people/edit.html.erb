<h1> <%= t(:editing) %> <%= t(:person)  %> </h1>

<%= render 'form' %>

<div class="preprend-5">
	<%= link_to t(:show), @person, :class => 'button' %>
	<%= link_to t(:back), people_path, :class => 'button' %>
</div>
